<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}ASMOTORS{% endblock %}</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
            <div class="container">
                <a class="navbar-brand" href="/">ASMOTORS</a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
                    <ul class="navbar-nav">
                        {% if session.user or session.admin %}
                            {% if session.admin %}
                                <li class="nav-item"><a href="/admin.php" class="nav-link">Admin</a></li>
                            {% endif %}
                            <li class="nav-item"><a href="/logout.php" class="btn btn-danger ms-2">Déconnexion</a></li>
                        {% else %}
                            <li class="nav-item"><a href="/register.php" class="btn btn-outline-light ms-2">Inscription</a></li>
                            <li class="nav-item"><a href="/login.php" class="btn btn-primary ms-2">Connexion</a></li>
                        {% endif %}
                    </ul>
                </div>
            </div>
        </nav>
    </header>

    <main>{% block content %}{% endblock %}</main>

    <footer>&copy; 2024 ASMOTORS - Tous droits réservés.</footer>
</body>
</html>
